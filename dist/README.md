# Distribution artifacts

`craft-audit.skill` is generated from the canonical source under [`skills/craft-audit/`](../skills/craft-audit/).

Build it from the repository root with:

```bash
python3 scripts/package_skill.py
```

Do not edit the packaged archive directly. Changes belong in `skills/craft-audit/` and should then be repackaged.
